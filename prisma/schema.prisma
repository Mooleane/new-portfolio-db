// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// TODO: Create your Project model here
// The model should include fields for:
// - id (Int, @id, @default(autoincrement()))
// - title (String)
// - description (String)
// - imageUrl (String, optional)
// - projectUrl (String, optional)
// - githubUrl (String, optional)
// - technologies (String[])
// - createdAt (DateTime, @default(now()))
// - updatedAt (DateTime, @updatedAt)

// Students should uncomment and implement this model:
model Project {
  id           Int      @id @default(autoincrement())
  title        String
  description  String
  imageUrl     String?
  projectUrl   String?
  githubUrl    String?
  technologies String[]
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
